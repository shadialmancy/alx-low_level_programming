#include "main.h"
/**
 * add - adds two integers and returns the result
 * @x: int to be add to y
 * @y: int to be add to x
 * Return: sum of a and b
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);
}
